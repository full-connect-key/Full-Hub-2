import { PagePlaceholder } from "@/components/page-placeholder";

export const metadata = { title: "Recomendações · Full Hub" };

export default function Page() {
  return <PagePlaceholder titulo="Recomendações" descricao="Recomendações registradas pela equipe." />;
}
