import { PagePlaceholder } from "@/components/page-placeholder";

export const metadata = { title: "Minhas solicitações · Full Hub" };

export default function Page() {
  return <PagePlaceholder titulo="Minhas solicitações" descricao="Suas solicitações de férias e ausências." />;
}
