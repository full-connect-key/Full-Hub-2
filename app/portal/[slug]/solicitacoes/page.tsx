import { PagePlaceholder } from "@/components/page-placeholder";

export const metadata = { title: "Solicitações · Full Hub" };

export default function Page() {
  return <PagePlaceholder titulo="Solicitações" descricao="Suas solicitações abertas com a agência." />;
}
